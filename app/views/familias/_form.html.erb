<%= form_for(@familia) do |f| %>
  <% if @familia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@familia.errors.count, "error") %> prohibited this familia from being saved:</h2>

      <ul>
      <% @familia.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :familia %><br />
    <%= f.text_field :familia %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
